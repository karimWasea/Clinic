namespace ClinicUtilities.SystemEnums
{
    public enum VisitType
    {
        RegularCheckup,
        Emergency,
        FollowUp,
     }

    public enum VisitStatus
    {
        Scheduled,
        InProgress,
        Completed,
        Canceled,
     }
    public enum Gender
    {
        Male, femail, other
    }
}
